[build]
  base = "autoai-concierge-main"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "VITE_HF_API_KEY,VITE_SUPABASE_URL,VITE_SUPABASE_ANON_KEY"